type Subject {
    id: ID!
    name: String!
}

input NewSubject {
    name: String!
}

type Query {
    findByName(name: String): Subject
    findAllSubjects: [Subject]
}

extend type Mutation {
    createSubject(input: NewSubject!): Subject!
}